import React from "react";
import styled from "styled-components";

const Home = () => {
  return (
    <HomePage>
      <h1>MeraJinn!</h1>
    </HomePage>
  );
};

export default Home;

// -----------STYLED COMPONENTS---------

const HomePage = styled.div`
  background: whites;
`;
